parameters:
    level: max
    paths:
        - src
        - tests
    #ignoreErrors:
    #    -
    #      message: '#Property [a-zA-Z0-9\\_]+::\$id is never written, only read.#'
    #      path: src/Entity/*
